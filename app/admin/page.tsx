import {isAdmin} from "@/lib/admin";import AdminWorkspace from "@/components/AdminWorkspace";
export default async function Admin(){if(await isAdmin())return <AdminWorkspace/>;return <main className="shell" style={{maxWidth:430,paddingTop:100}}><div className="card track-content stack"><div className="brand">Creator login</div><p className="muted">This workspace is for the circuit developer.</p><form action="/admin/login" method="post" className="stack"><input className="field" name="passphrase" type="password" placeholder="Passphrase" required/><button className="btn">Unlock workspace</button></form></div></main>}
