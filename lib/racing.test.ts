import {describe,it,expect} from "vitest";import {progressRace,isPersonalBest,validateRun} from "./racing";
const start={id:"s",label:"Start",lat:0,lng:0}, cp={id:"c",label:"CP",lat:0,lng:.001};
describe("race progression",()=>{it("arms inside start, starts outside, requires checkpoint exit, then finishes inside start",()=>{let s={phase:"ready" as const,nextCheckpoint:0,sequence:[] as string[]};s=progressRace(s,{lat:0,lng:0},start,[cp],1000) as any;expect(s).toMatchObject({phase:"ready",startArmed:true});s=progressRace(s,{lat:0,lng:-.001},start,[cp],2000) as any;expect(s).toMatchObject({phase:"racing",startedAt:2000,sequence:["s"]});s=progressRace(s,{lat:0,lng:.001},start,[cp],3000) as any;expect(s).toMatchObject({nextCheckpoint:0,checkpointEntered:true});s=progressRace(s,{lat:0,lng:.002},start,[cp],4000) as any;expect(s).toMatchObject({nextCheckpoint:1,sequence:["s","c"]});s=progressRace(s,{lat:0,lng:0},start,[cp],5000) as any;expect(s).toMatchObject({phase:"complete",elapsedMs:3000,sequence:["s","c","s"]})});it("only replaces a PB with a quicker time",()=>{expect(isPersonalBest(1000,999)).toBe(true);expect(isPersonalBest(1000,1001)).toBe(false)});it("rejects forged sequences",()=>expect(validateRun({trackId:"x",displayName:"a",elapsedMs:12000,accuracyM:3,maxSpeedMps:4,gateSequence:["s","s"]},["s","c","s"])).toBeTruthy())});
