"use client";
import {MapContainer,TileLayer,Polyline,CircleMarker,useMap,useMapEvents} from "react-leaflet";
import {useEffect} from "react";
import type {Gate,Point} from "@/lib/types";
function Clicker({onClick}:{onClick?:(p:Point)=>void}){useMapEvents({click:e=>onClick?.(e.latlng)});return null}
function LocateControl(){const map=useMap();const locate=()=>{if(!navigator.geolocation){window.alert("GPS location is not supported by this browser.");return}navigator.geolocation.getCurrentPosition(position=>map.flyTo([position.coords.latitude,position.coords.longitude],17,{animate:true}),error=>window.alert(error.code===1?"Location access was denied. Enable it in your browser settings and try again.":"Your location is unavailable. Try again with a clearer GPS signal."),{enableHighAccuracy:true,maximumAge:15_000,timeout:15_000})};return <button type="button" className="map-locate" onClick={locate} aria-label="Pan map to my current location">◎ My location</button>}
function Viewport({points}:{points:Point[]}){const map=useMap();useEffect(()=>{if(!points.length)return;if(points.length===1){map.setView(points[0],15);return}map.fitBounds(points.map(point=>[point.lat,point.lng] as [number,number]),{padding:[32,32],maxZoom:16})},[map,points]);return null}
export default function TrackMap({route=[],start,checkpoints=[],editable,onMapClick,className=""}:{route?:Point[];start?:Gate;checkpoints?:Gate[];editable?:boolean;onMapClick?:(p:Point)=>void;className?:string}){const center=start||route[0]||{lat:51.505,lng:-.09};const circuitPoints=[...(start?[start]:[]),...route,...checkpoints];return <MapContainer className={`map ${className}`} center={center} zoom={15} scrollWheelZoom><TileLayer attribution='&copy; <a href="https://www.openstreetmap.org/copyright">OpenStreetMap</a> contributors' url="https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png"/><Viewport points={circuitPoints}/>{route.length>1&&<Polyline positions={route} color="#d8ff45" weight={5}/>} {start&&<CircleMarker center={start} radius={12} pathOptions={{color:"#fff",fillColor:"#d8ff45",fillOpacity:1}}/>}{checkpoints.map(g=><CircleMarker key={g.id} center={g} radius={10} pathOptions={{color:"#fff",fillColor:"#fb933a",fillOpacity:1}}/>)}<LocateControl/>{editable&&<Clicker onClick={onMapClick}/>}</MapContainer>}
